//Todos: link shrek api (find key?), 
// write function to search the api for a random quote on button press (click event listener?), 
// add clear button/add to fave button?
// if so would need to use local storage & that's kind of a pain...
var quoteSpace = docutment.getElementById("quoteSpace");
var searchBtn = document.getElementById("searchBtn");